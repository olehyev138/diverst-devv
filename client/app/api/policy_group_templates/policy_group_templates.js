import API from 'api/base/base';
const axios = require('axios');

const PolicyGroupTemplates = new API({ controller: 'policy_group_templates' });

Object.assign(PolicyGroupTemplates, {
});

export default PolicyGroupTemplates;
