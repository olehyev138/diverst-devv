/*
 * Error page Messages
 *
 * This contains all the error page messages
 */

import { defineMessages } from 'react-intl';

export const scope = 'diverst.components.ErrorBoundary';

export default defineMessages({
  home: {
    id: `${scope}.home`,
  },
  message: {
    id: `${scope}.message`,
  }
});
