/*
 * SSOLandingPage Messages
 *
 * This contains all the text for the SSOLandingPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'diverst.containers.SSOLanding';

export default defineMessages({
  enter: {
    id: `${scope}.input.enter`,
  },
});
