/*
 * Diverst Logout dialog Messages
 *
 * This contains all the text for the Logout dialog component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'diverst.components.DiverstLogoutDialog';

export default defineMessages({
  later: {
    id: `${scope}.later`,
  },
  logout: {
    id: `${scope}.logout`,
  },

});
