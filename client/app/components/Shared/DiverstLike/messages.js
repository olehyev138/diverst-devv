/*
 * Like Messages
 *
 * This contains all the text for the Like component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'diverst.components.DiverstLike';

export default defineMessages({
  color: {
    id: `${scope}.color`,
  }
});
