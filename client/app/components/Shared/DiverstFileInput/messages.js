/*
 * FileInput Messages
 *
 * This contains all the text for the FileInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'diverst.components.DiverstFileInput';

export default defineMessages({
  replace: {
    id: `${scope}.replace`,
  },
  choose: {
    id: `${scope}.choose`,
  },
  uploading: {
    id: `${scope}.uploading`,
  },
  waiting: {
    id: `${scope}.waiting`,
  },
  error: {
    id: `${scope}.error`,
  },
  finished: {
    id: `${scope}.finished`,
  },
  nofile: {
    id: `${scope}.nofile`,
  },
  deleteFileConfirm: {
    id: `${scope}.deleteFileConfirm`
  },
  delete: {
    id: `${scope}.delete`
  },
  fileTypes: {
    id: `${scope}.fileTypes`,
    png: {
      id: `${scope}.fileTypes.png`,
    },
    jpg: {
      id: `${scope}.fileTypes.jpg`,
    },
    jpeg: {
      id: `${scope}.fileTypes.jpeg`,
    },
    svg: {
      id: `${scope}.fileTypes.svg`,
    },
    gif: {
      id: `${scope}.fileTypes.gif`,
    },
    pdf: {
      id: `${scope}.fileTypes.pdf`,
    },
    xml: {
      id: `${scope}.fileTypes.xml`,
    },
    csv: {
      id: `${scope}.fileTypes.csv`,
    },
    json: {
      id: `${scope}.fileTypes.json`,
    },
    zip: {
      id: `${scope}.fileTypes.zip`,
    },
    plain: {
      id: `${scope}.fileTypes.plain`,
    },
    html: {
      id: `${scope}.fileTypes.html`,
    },
  },
  notAccepted: {
    id: `${scope}.notAccepted`
  },
});
