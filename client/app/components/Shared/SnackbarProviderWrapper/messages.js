/*
 * SnackbarProvider Messages
 *
 * This contains all the text for the SnackbarProvider component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'diverst.components.SnackbarProviderWrapper';

export default defineMessages({
  close: {
    id: `${scope}.close`,
  }
});
