/*
 * Pagination Messages
 *
 * This contains all the text for the Pagination component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'diverst.components.DiverstPagination';

export default defineMessages({
  first: {
    id: `${scope}.first`,
  },
  last: {
    id: `${scope}.last`,
  },
  next: {
    id: `${scope}.next`,
  },
  prev: {
    id: `${scope}.prev`,
  },
});
