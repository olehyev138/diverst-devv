/*
 * ColorPicker Messages
 *
 * This contains all the text for the ColorPicker component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'diverst.components.DiverstColorPicker';

export default defineMessages({
  color: {
    id: `${scope}.color`,
  }
});
