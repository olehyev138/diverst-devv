/**
 *
 * Asynchronously loads the component for EnterpriseConfiguration
 *
 */

import loadable from 'utils/loadable';

export default loadable(() => import('components/GlobalSettings/EnterpriseConfiguration/index'));
