<%= simple_form_for @topic do |f| %>
  <%= f.input :statement, input_html: { autofocus: true } %>
  <%= f.input :expiration, default: Time.current + 1.month %>
  <%= f.submit "Create topic", class: "btn btn--primary" %>
<% end %>
