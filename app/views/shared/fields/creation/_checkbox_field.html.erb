<%= f.input :title, label: local_assigns.fetch(:label, "Title") %>
<%= f.input :options_text, as: :text, label: "Options (one per line)" %>
<%= f.hidden_field :alternative_layout, value: 0 %>
<%= f.input :alternative_layout, label: "Use multi-select field" %>
