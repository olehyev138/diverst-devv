Tried to import <%= @count %> rows.
<br>
There were <%= @successful_rows.count %> successful uploads and <%= @failed_rows.count %> errors

<% if @failed_rows.count > 0 %>
Errors list:
  <% @failed_rows.each do |row| %>
    <hr>
    Row: <%= row[:row] %>
    <br>
    Index: <%= row[:row_index] %>
    <br>
    Error: <%= row[:error] %>
  <% end %>
<% end %>