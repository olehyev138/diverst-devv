<% if @group.banner.present? %>
  <div class="row">
    <div class="col sm-12">
      <%= image_tag(@group.banner.url, alt: @group.banner_description, style: 'width: 100%; max-width: 100%;') %>
    </div>
  </div>
<% end %>