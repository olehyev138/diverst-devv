<div class="content__header">
  <h1>
    <%= link_to "Events", group_events_path(@group) %> /
    <%= @event.title %>
  </h1>
  <%= link_to "Edit event", edit_group_event_path(@group, @event), class: "btn btn--primary" if current_user.is_a? Admin %>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">

    </div>
  </div>
</div>