<div class="content__header">
  <h2 class="h1-styling">Add Members to <%= @group.name %></h2>
</div>

<div class="content__main">
  <%= simple_form_for @group, :url => add_members_group_group_members_path(@group), :method => :post do |f| %>
    <div class="card">
      <div class="card__section card__section--border">
        <%= f.association :members,
            as: :select, 
            input_html: { class: "select2-field-new-members", autofocus: true },
            selected: [],
            collection: [],
            label: 'Select new members'
        %>
      </div>
    </div>

    <%= f.button :submit %>
  <% end %>
</div>

<%= render partial: 'shared/select2_ajax_search.js.erb' %>