<div>
  <%= link_to 'Delete', user_social_link_path(post.id),method: :delete, class: "error", data: { confirm: "Are you sure?" } if GroupSocialLinkPolicy.new(current_user, [post.group, post]).destroy? %>
  <%= raw post.embed_code %>
</div>