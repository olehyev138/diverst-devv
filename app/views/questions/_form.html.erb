<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for [@campaign, @question] do |f| %>
        <%= f.input :title %>
        <%= f.input :description %>
        <%= f.button :submit %>
      <% end %>
    </div>
  </div>
</div>