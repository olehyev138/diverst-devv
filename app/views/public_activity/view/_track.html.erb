<div class="card__section">
  <%= render 'public_activity/shared/owner', activity: activity %>
  <% if activity.trackable %>
     <% if activity.trackable.resource.group %>
     	 viewed a group resource
	 <% elsif activity.trackable.resource.enterprise %>
	 	 viewed an enterprise resource
	 <% end %>    
  <% else %>
    which has since been removed
  <% end %>
  (<%= activity.created_at.to_s :datetime %>)
</div>