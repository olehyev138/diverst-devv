<% if activity.owner %>
  <%= image_tag activity.owner.avatar.expiring_url(3600, :thumb), width: '20px'%>
  <%= link_to activity.owner.name, user_user_path(activity.owner) %>
<% else %>
  Unknown user
<% end %>