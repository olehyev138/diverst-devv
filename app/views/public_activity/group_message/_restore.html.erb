<div class="card__section">
  <%= render 'public_activity/shared/owner', activity: activity %>
  restored a group message
  <% if activity.trackable %>
    <%= link_to activity.trackable.subject, group_group_message_path(activity.trackable.group, activity.trackable) %>
  <% else %>
    which has since been removed
  <% end %>
  (<%= activity.created_at.to_s :datetime %>)
</div>