<div class="card__section">
  <%= render 'public_activity/shared/owner', activity: activity %>
  exported event attendees
  <% if activity.trackable %>
    <%= link_to activity.trackable.title, group_event_path(activity.trackable.group, activity.trackable) %>
  <% else %>
    which has since been removed
  <% end %>
  (<%= activity.created_at.to_s :datetime %>)
</div>