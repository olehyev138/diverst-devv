<div class="card__section">
  <%= render 'public_activity/shared/owner', activity: activity %>
  archived an initiative
  <% if activity.trackable %>
    (<%= link_to activity.trackable.name, group_event_path(activity.trackable.owner_group, activity.trackable) %>)
  <% else %>
    which has since been removed
  <% end %>
  (<%= activity.created_at.to_s :datetime %>)
</div>