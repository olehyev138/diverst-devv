<%= render 'public_activity/shared/owner', activity: activity %>
created event
<% if activity.trackable %>
  <%= link_to activity.trackable.name, group_event_path(activity.trackable.group, activity.trackable) %>
<% else %>
  which has since been removed
<% end %>