<div class="content__header">
  <h2 class="h1-styling">Admin Resources</h2>
  <%= link_to "+ Add resource", polymorphic_path(@container_path + [:resource], action: "new", resource_type: "admin"), class: "btn btn--primary" if EnterpriseFolderPolicy.new(current_user, nil).new? %>
</div>

<%= render 'table', resources: @admin_resources, name: "admin" %>

<div class="content__header">
  <h2 class="h1-styling">National Resources</h2>
  <%= link_to "+ Add resource", polymorphic_path(@container_path + [:resource], action: "new", resource_type: "national"), class: "btn btn--primary" if EnterpriseFolderPolicy.new(current_user, nil).new? %>
</div>

<%= render 'table', resources: @national_resources, name: "national"%>