<div class="content__header">
  <h1>Branding</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, url: update_branding_enterprise_path(@enterprise, source: 'edit_branding'), html: {id: 'edit_enterprise_branding'} do |f| %>
        <%= f.simple_fields_for @theme do |t| %>
          <div id="colors_row" class="row">
            <div id='primary_color_column' class="col sm-6">
              <%= t.input :primary_color, id: 'primary_color',input_html: { class: 'jscolor', autofocus: true } %>
            </div>
            <div class="col sm-6">
              <%= f.hidden_field :use_secondary_color, value: 0 %>
              <%= t.input :use_secondary_color, id: 'use_secondary_color', label: 'Use different color for charts' %>
              <%= t.input :secondary_color, id: 'secondary_color', input_html: { class: 'jscolor' }, label: 'Charts color' %>
            </div>
          </div>
          <%= t.input :logo, as: :attachment_preview %>

          <%= t.input :logo_redirect_url %>

          <%= t.button :submit, "Save branding" %>
          <%= link_to "Restore to default", restore_default_branding_enterprise_path(@enterprise), class: "btn btn--secondary", method: :patch %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__header">
  <h1>User Home view</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
        <%= f.label :banner, label: "User Home banner", input_html: { class: "field__label" } %>
        <% if @enterprise.banner? %>
          <div class="flex-row__cell fright">
            <%= link_to "Remove", delete_attachment_enterprise_path(@enterprise, attachment: :banner), method: :patch, class: "error", data: { confirm: "Are you sure?" } %>
          </div>
        <% end %>
        <%= f.input :banner, as: :attachment_preview, label: false %>

        <%= f.label :home_message, label: "User Home message", input_html: { class: "field__label" } %>
        <%= f.input :home_message, label: false %>

        <%= f.button :submit, "Save user settings" %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
        <%= f.input :time_zone %>

        <%= f.button :submit, "Save timezone" %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__header">
  <h1>Edit Program Sponsor details</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
      <%= f.input :cdo_name, label: "Sponsor name", placeholder: "Jane Doe" %>
      <%= f.input :cdo_title, label: "Sponsor title", placeholder: "VP Diversity & Inclusion" %>
      <%= f.input :cdo_message_email, label: "Home page sponsor message" %>
      <%= f.hidden_field :disable_sponsor_message, value: false %>
      <%= f.input :disable_sponsor_message,
      label: "disable sponsor message?",
      input_html: { autofocus: true } %>
      <%= f.input :cdo_message, label: "Onboarding sponsor message" %>
      <%= f.input :sponsor_media, label: "Upload a sponsor image or video" %>
      <%= f.input :onboarding_sponsor_media, label: "Upload onboarding video" %>

        <%= f.input :privacy_statement %>

        <%= f.button :submit, "Save sponsor info" %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__header">
  <h1>Edit User Group Notification Mailer Text</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
      
        <div class="card__section card__section--border">
          <%= f.input :user_group_mailer_notification_text,
              as: :ckeditor
          %>
        </div>

        <%= f.button :submit, "Save User Group Mailer text" %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__header">
  <h1>Edit Campaign Invitation Mailer Text</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
      
        <div class="card__section card__section--border">
          <%= f.input :campaign_mailer_notification_text,
              as: :ckeditor
          %>
        </div>

        <%= f.button :submit, "Save Campaign Invitation Mailer text" %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__header">
  <h1>Edit Approve Budget Mailer Text</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
      
        <div class="card__section card__section--border">
          <%= f.input :approve_budget_request_mailer_notification_text,
              as: :ckeditor
          %>
        </div>

        <%= f.button :submit, "Save Approve Budget Mailer text" %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__header">
  <h1>Edit Survey Mailer Text</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
      
        <div class="card__section card__section--border">
          <%= f.input :poll_mailer_notification_text,
              as: :ckeditor
          %>
        </div>

        <%= f.button :submit, "Save Survey Mailer text" %>
      <% end %>
    </div>
  </div>
</div>

<div class="content__header">
  <h1>Edit Budget Approved Mailer Text</h1>
</div>

<div class="content__main">
  <div class="card">
    <div class="card__section">
      <%= simple_form_for @enterprise, :url => enterprise_path(@enterprise, source: 'edit_branding') do |f| %>
      
        <div class="card__section card__section--border">
          <%= f.input :budget_approved_mailer_notification_text,
              as: :ckeditor
          %>
        </div>

        <%= f.button :submit, "Save Budget Approved Mailer text" %>
      <% end %>
    </div>
  </div>
</div>