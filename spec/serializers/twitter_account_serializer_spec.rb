require 'rails_helper'

RSpec.describe TwitterAccountSerializer, type: :serializer do
  it 'returns twitter account' do
    # twitter_account = create(:twitter_account)
    # serializer = TwitterAccountSerializer.new(twitter_account, scope: serializer_scopes(create(:user)), scope_name: :scope)

    # expect(serializer.serializable_hash[:id]).to eq(twitter_account.id)
  end
end
