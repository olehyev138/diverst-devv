machine:
  timezone:
    America/New_York
  services:
    - elasticsearch
deployment:
  production:
    branch: production
    commands:
      - bundle exec cap production deploy
  staging:
    branch: development
    commands:
      - bundle exec cap staging deploy